{
  "name": "c2a-lap",
  "private": true,
  "version": "1.0.0",
  "description": "Proprietary sales and storefront platform for C2A LAP only.",
  "author": "Mohamed Diaa El Deen Samy (Freelancer)",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix client install",
    "dev": "npm run build && npm --prefix server run dev",
    "build": "npm --prefix server install && npm --prefix client install && npm --prefix client run build",
    "start": "node server/src/server.js",
    "windows:dev": "cmd /c run-app.bat",
    "windows:build": "cmd /c build-client.bat",
    "windows:start": "cmd /c run-server.bat"
  },
  "devDependencies": {}
}
